// Define a generic interface for the data structure
export interface IQuickReplyTableHeaderProps {
  id?: string | number;
  title: string;
  created_by?: string;
  created_on?: string;
  quick_reply: string;
}
